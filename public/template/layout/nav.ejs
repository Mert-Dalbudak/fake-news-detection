<div id="nav-container">
    <div id="nav">
        <ul>
            <? for(let i = 0; i < nav_schema.length; i++) { ?>
            <li id="<?= nav_schema[i].id ?>">
                <div class="nav-link">
                    <a href="<?= nav_schema[i].href ?>">
                        <span class="icon <?= nav_schema[i].icon ?>"><?= __(nav_schema[i].title) ?></span>
                    </a>
                    <? if(nav_schema[i].children && nav_schema[i].children.length > 0) {?>
                    <button class="context-menu-link-arrow"></button>
                    <? } ?>
                </div>
                <? if(nav_schema[i].children) {?>
                <div class="nav-link-children-container hidden">
                    <ul class="nav-link-children">
                    <? for(let j = 0; j < nav_schema[i].children.length; j++) {?>
                        <li id="<?= nav_schema[i].children[j].id ?>" class="nav-link-child">
                            <div class="nav-link">
                                <span class="icon <?= nav_schema[i].children[j].icon ?>"><?= __(nav_schema[i].children[j].title) ?></span>
                            </div>
                        </li>
                    <? } ?>
                    </ul>
                </div>
                <? } ?>
            </li>
            <? } ?>
        </ul>
        <div id="navFooter">
            <div id="navFooter-social">
            
            </div>
            <div id="navFooter-links">
                <a target="_blank" href="<?= __('footer_contact_us_url') ?>"><?= __('footer_contact_us') ?></a>|<a target="_blank" href="<?= __('footer_legal_notice_url') ?>"><?= __('footer_legal_notice') ?></a><!--|<a target="_blank" href="<?= __('footer_about_fh-aachen_url') ?>"><?= __('footer_about_fh-aachen') ?></a>-->
            </div>
            <div id="copyright">
                © <?= new Date().getFullYear() ?> Mert Dalbudak
            </div>  
        </div>
    </div>
</div>